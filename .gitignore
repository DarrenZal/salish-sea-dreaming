# TouchDesigner
*.toe.autosave
*.toe.crash
Backup/

# macOS
.DS_Store
*.swp

# Python
__pycache__/
*.pyc

# Node.js / Web
node_modules/
web/dist/
web/.vite/

# Large generated assets (keep local only)
assets/output/
assets/reference/
tmp/
.env
.ssd_secrets.ps1
.ssd_alert_state.json

# Collaborator source media and local extracts (large, not committed)
media/collaborators/
media/hero-subclips/
media/sequences/
images/moonfish-frames/
images/moonfish-preview/
images/denning-ppt-raw/
images/denning-ppt-preview/
austin-reference/
images/denning-curated/

# Training zip archives (for TELUS upload, not committed)
*.zip

# Raw scraped images (large, not committed)
images/*-raw/
images/*-openverse/
images/*-openverse-raw/
images/*-targeted/
images/*-wikimedia/

# Generated exhibition output (3.8GB of videos, regenerable)
output/

# Prepped training corpora (512x512, regenerable from raw via prep_training_data.py)
training-data/fish-model/
training-data/whale-model/
training-data/bird-model/

# Legacy/archived corpora
training-data/fish-model-legacy-unsafe-*/

# Trained model checkpoints (~347 MB each, share via file transfer)
models/

# TELUS fakes grids (~60MB each, keep local only)
telus/fakes*.png

# Playwright MCP logs
.playwright-mcp/

# LoRA generated artifacts (large, regenerable from scripts)
briony-lora/gan-frames/
briony-lora/eval/img2img/
briony-lora/eval/temporal/
briony-lora/curate/
briony-lora/images/
briony-lora/*.safetensors

# AdaIN experimentation (local only)
adain-test/

# Top-level image scratch files
boids-*.png
qr_gallery.png

# Excalidraw logs
excalidraw.log

# QC contact sheets (large PNGs, regenerable from raw images)
training-data/review/contact-sheets/

# Provenance backup
training-data/provenance.csv.bak

# Claude-mem auto-generated activity stubs (not the canonical /CLAUDE.md)
briony-lora/CLAUDE.md
docs/CLAUDE.md
docs/archive/CLAUDE.md
docs/archive/drafts/CLAUDE.md
docs/comms/CLAUDE.md
scripts/CLAUDE.md
tools/CLAUDE.md
training-data/CLAUDE.md
training-data/review/CLAUDE.md
VisualArt/CLAUDE.md
web/src/CLAUDE.md

# Claude Code internals (worktrees, scratchpads)
.claude/

# Private / sensitive — DO NOT commit to public repo
docs/Wasanec Legal Case/
docs/comms/2026-03-24-signal-response-draft.md
docs/comms/2026-03-25-signal-update-draft.md

# Corpus + training data (large, regenerable, host out-of-band)
training-data/dreaming-model-512/
training-data/supplement-512/
training-data/review/qc-*/
training-data/review/rejects-*.csv
training-data/review/tags-*.csv
training-data/review/balanced-selection.csv
tools/species-supplement.tsv

# TELUS training runs (outputs kept locally / shared via Drive)
telus/stats-*.jsonl
telus/log-*.txt
telus/training_options-*.json
telus/ssd_experiments_*.py

# StyleGAN2 source tree (vendored locally, not our code)
stylegan2/

# Generated school-marketing output (regenerable from scripts/generate_school_marketing*.py)
output/school-marketing/
tools/mediapipe/*.tox

# iNat species thumbnail corpus — cached/regenerable from scripts/, no reason to track
static/thumbs/

# v2 ingest rehearsal directory — workflow proof, not real Austin material
austin-v2-ingest-rehearsal/

# Austin curated source files and generated morph renders — private/local only
austin-v2-ingest/
track2-deterministic/morph_outputs/
track2-deterministic/morph_outputs_INTERNAL/

# Private operator archives (visitor dreams, consent-sensitive) — never commit
private-archive/

# --- Post-IMPACT consolidation 2026-06-10 ---

# Session scratch (screenshots, debug captures, overnight test dirs)
.tmp-*
v2-mudra-debug-panel.png
v6-dreamworld-impact-mobile.png
ssd-graph-v6-after-fix.png
memex-configurator-2026-05-16.png

# Bob Turner research corpus (230 MB media — host out-of-band)
bob-turner-corpus/

# TouchDesigner working files (3 GB: frame sequences, .tox checkpoints)
td/

# Track 2 morph engine renders (67 GB, regenerable) — keep only morph_pairs.csv (force-added)
track2-deterministic/

# Autolume sweep render artifacts (PNG/video, regenerable; misfiled under docs/)
docs/space-center/autolume-sweep-2026-05-21/
docs/space-center/autolume-sweep-2026-05-21-v2/
docs/space-center/autolume-wild30s-2026-05-21/
