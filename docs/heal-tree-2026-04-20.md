# Heal Tree — 2026-04-20 (during Salt Spring opening week)

State of auto-recovery for Salish Sea Dreaming at Mahon Hall. What
heals without human intervention, what still needs a human, and
what fixes are queued. Snapshot as of **Apr 20 post-opening**, with
exhibition running April 10–26.

## What auto-heals today

| Component | Detection | Recovery | Verified |
|---|---|---|---|
| **Resolume Arena process** | `SSD-Resolume-Watchdog-v2` checks every ~60s for running `Arena.exe` | Fires `SSD-Resolume` task → `launch_resolume.bat` → Arena.exe with composition file arg | ✅ Fired live at 09:49 and 11:19 today |
| **Projector assignment after Arena restart** | Auto-wired in watchdog: waits 20s after restart, then fires `SSD-Resolume-Kick` (Ctrl+Shift+A) | AHK `resolume_kick.ahk` uses `WinActivate` + `Send` (SendInput API) to reach Arena regardless of focus | ⚠️ AHK swap deployed 11:34, **still untested end-to-end** — prior PowerShell `SendKeys` version failed silently due to Windows foreground-lock protection |

## What does NOT auto-heal (known gaps)

### Critical — blocks unattended overnight recovery

| Gap | Observed | Impact | Status |
|---|---|---|---|
| **Arena startup prompt** | "Do you really want to open a composition?" dialog appears on cold start (observed twice today — after manual Resolume shutdown at 11:19 and 11:22) | Modal blocks composition load → projectors stay blank → operator must click through | Research findings: (a) try `--suppressCompositionChangeConfirmation` CLI flag — undocumented but mentioned on Resolume forums; (b) check for + pre-delete crash recovery file before each launch (location uncertain — inspected `%APPDATA%\Local\Resolume Arena\` and `Documents\Resolume Arena\Preferences\` on 3090, no obvious `recovery.avc` found); (c) Resolume has no documented kiosk mode. **Next step:** update `launch_resolume.bat` locally with the suppression flag, test when gallery closes tonight. |
| **SSH tunnel auto-recovery** | `SSD-SSH-Tunnel` task launches tunnel on logon, but if the tunnel dies mid-session (as happened Apr 19 at 16:50 PDT), there's no auto-restart | No remote access until someone at the 3090 keyboard re-triggers the task | No fix yet. Needs a tunnel-specific watchdog (check `ss`/`netstat` or TCP reachability to poly, restart on failure). Was Phase 0 priority in the resilience plan; hit by today's outage. |
| **Ableton live audio source** | Not launched automatically on boot; no watchdog | If Ableton crashes overnight or doesn't start, gallery goes silent | Plan per task #3: switch automatic-mode audio from Ableton to Windows Media Player loop (more reliable, auto-start via task). Audio detector needs WASAPI loopback rewrite (task #10). |

### Non-critical (nice-to-have improvements)

| Gap | Status |
|---|---|
| Autolume auto-restart | `SSD-Autolume` task exists but no watchdog. Autolume reported 8.7 FPS by Prav this morning — perf degradation, not crash. Deferred. |
| TouchDesigner watchdog | `SSD-TouchDesigner` task exists for boot launch; no crash watchdog. TD has been stable (uptime 22h as of mid-morning). |
| BenQ EDID stability | Dongle installed today; resolution recovered to 1920×1080 after a "reset" but long-term stability under projector power-cycle unverified. **Diagnostic test needed (next time Prav is on site):** with PC running, power BenQ off or unplug the HDMI downstream of the dongle. If Windows keeps 1920×1080 display active → EDID latched in emulation (good). If display disappears → still in passthrough (bad, will fail overnight). If passthrough: hold learn button 5–10s with BenQ on + live, wait for LED, retest. If no learn button: swap to Gefen EXT-HD-EDIDPN or DrHDMI 4K (both proven in unattended gallery installs). |
| Silence detection | Current `gallery_audio.py` reads `AUDIO_DEVICE_INDEX=0` (input-side) but 3090 has no mic. Alerts unreliable. Needs rewrite to WASAPI loopback on default playback device. |
| Meta-watchdog | `heartbeat_manifest.json` deployed, but no process yet reads it to detect stalled watchdogs. Phase 1 of the resilience plan. |

## What we proved today

- **Resolume watchdog v2 works**: detected Arena down at 09:49 and 11:19, both times restarted within ~1 min.
- **WireGuard backup tunnel works**: Mac `10.100.0.2` ↔ 3090 `10.100.0.30` via poly hub. Independent of the SSH tunnel. If SSH tunnel dies, WG still reachable (verified with `ssh user@10.100.0.30`).
- **Windows Update fully blocked** (5 layers: registry + pause + WU tasks + NVIDIA GFE + metered-pending). No surprise reboots for ≥35 days.
- **Daily Diagnostic scheduled for 09:15 AM** via `SSD-Daily-Diagnostic`. Telegram-delivered. Email pending Prav's Gmail app password.

## What Prav proved today (manual test at ~11:18)

Simulated overnight cycle: powered projectors off, powered them on, shut down Resolume. Observations:

- Arena watchdog auto-restarted Arena ✅
- BUT Arena showed the startup prompt — Prav had to click through manually ❌
- AND projectors did not auto-assign — Prav had to open Advanced Output manually and click "Back Wall" ❌
- Display re-enumeration briefly "went wonky" during projector power-on (suspected dongle/EDID instability)

**Conclusion:** auto-recovery currently unreliable for cold overnight cycles. Three fixes in flight (Arena prompt suppression, AHK-based kick, EDID dongle validation).

## Queued fixes (ordered by impact)

1. **Arena startup prompt suppression** — implement `--suppressCompositionChangeConfirmation` flag in `launch_resolume.bat` + pre-launch delete of any recovery state file. Test tonight when gallery closes.
2. **Validate AHK kick works** — kill Arena, let watchdog restart, confirm Advanced Output opens + projectors assign without human intervention. Must happen when no visitors present.
3. **Dongle EDID state verification** — confirm whether current dongle is in "pass-through" or "learned EDID" mode. Research pending.
4. **SSH tunnel watchdog** — add TCP reachability probe + auto-restart on the 3090. Phase 0 priority.
5. **Switch Ableton → WMP for automatic-mode audio** — simpler + more reliable loop.
6. **WASAPI loopback rewrite of `gallery_audio.py`** — silence detection finally working.
7. **Meta-watchdog** — read `heartbeat_manifest.json`, alert on stalled watchdogs.

## Human-callable state table for Zoe / gallery attendant

If the **wall goes blank**:
- Check if Arena window is visible on the 3090 desktop.
- If yes: press **Ctrl+Shift+A** to open Advanced Output. Projectors should assign.
- If still blank after 30s: click **Back Wall** in Advanced Output manually.
- If Arena is missing entirely: wait 2 min (watchdog restarts it), then press Ctrl+Shift+A.
- If nothing works after 5 min: call Darren (518-210-2828).

If **audio goes silent**:
- Check if Ableton is running on the 3090 desktop.
- If not running: launch Ableton from Start menu.
- If running but no sound: check speaker cables / Bluetooth pairing.
- If unclear: call Darren.

If you **get a Telegram alert** with `[!] [CRITICAL]` or `[!] [WARN]`:
- Only Darren gets these; Zoe doesn't need to act on them.
