H6 in HAP MOV is up on Proton:

<PASTE PROTON LINK HERE>

Specs: 3840×2160 / 60fps / HAP codec in MOV container / 7.7 GB / 50s. Large file so it may take a few minutes to download on your side.

Why HAP: it's Resolume's #2 recommended codec (after their proprietary DXV that needs Alley to encode), GPU-decoded so playback is smooth. Drop it directly into a Resolume composition — no Alley transcode needed.

Note: HAP won't preview in QuickTime (Apple doesn't natively support it), but Resolume and VLC handle it cleanly. If it loads in Resolume, you're set.

Fallback options if HAP doesn't work for any reason:
- ProRes 422 HQ MOV at 4K (~2.7 GB, smaller, plays in QuickTime + Resolume, CPU decode so slightly less smooth scrubbing)
- HAP MOV at 30fps (~3.8 GB, half the framerate, half the size — projection-grade)
- Just give me the word and I'll re-encode + re-upload.
