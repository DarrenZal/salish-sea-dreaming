# Austin/Prav — Drive received message

> Paste-ready Signal message to send to Pravin (and forward to Austin) the moment Austin's curated Drive folder lands. Sets expectations: triage → caption → internal v2 LoRA test → nothing public without per-output OK.
>
> **Status:** held until Austin's drive lands tonight.

---

**Channel:** Sen̓áḵw Dreaming Signal chat (Austin–Pravin–Darren)

**Message:**

Got the Drive folder, thank you. I'm going to treat the files as source material under the protocol framework: first triage for clean design / source plates, then caption at element level, then run an internal v2 style-transfer LoRA test. Nothing from this process goes public or to projectors without Austin's per-output OK.

In parallel, I'll start identifying which pieces are candidates for deterministic primitive decomposition / morphing. For that track I'll prepare a screenshare worksheet so Austin can confirm primitive labels, the "three shapes" in the pearl, and any no-go / restricted motifs before anything is rendered externally.

I'll send a quick first-look (internal recipe-proof only — not for forwarding) once the v2 LoRA finishes its first pass, plus a short list of pieces I think are best primitive-decomposition candidates for Austin to react to.

— Darren

---

## Send checklist (when drive lands)

- [ ] Confirm drive is fully downloaded + unzipped before sending — don't message and then discover files are still uploading
- [ ] Drop into `austin-v2-ingest/inbox/`
- [ ] Run `python3 scripts/v2_ingest_helper.py` triage; confirm count of approved-candidate plates
- [ ] If <15 approved plates: defer the message; ask Pravin first whether more are coming or this is the full set
- [ ] If 15+: send the message above
- [ ] Begin executing `austin-v2-ingest/FIRST_HOUR_AFTER_DRIVE_LANDS.md` checklist

## Tone notes

- Posture: receiving, not announcing. "Got the folder, thank you" — not "I'm now executing phase 2."
- Per-output OK is named explicitly. Per `feedback_austin_consent_trust_floor.md`: per-output approval is the floor, not the milestone.
- "Internal recipe-proof only — not for forwarding" matches the v1.5 finding that v2 generated outputs (even when watermark-free) are recipe verification, not candidate art.
- "Three shapes in the pearl" cites Austin's own May 11 framing — keeps his vision as the dramaturgical container.
- No timing commitments — "I'll send a quick first-look" without ETA. v2 LoRA pass is ~60 min wall-clock but caption labour is harder to predict.
