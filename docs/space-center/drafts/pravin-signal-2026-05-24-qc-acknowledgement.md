Honest answer on the process question: yes, I review before send — that's the design. On the autolume 22-min, I didn't play it end-to-end before sending the link. Checked metadata only. Missed both the noise_anim jumpy motion AND didn't anticipate the AVI conversion blowing up Alley + Resolume on your end. That cost you real composition time. Owning it.

Tightening going forward: play-the-file QC + downstream import-test on my Mac before any video deliverable. HAP-pre-encoded for Resolume-bound files, not raw H.264 expecting you to transcode.

Re-uploading the v001_3 overlay_black.mp4 (52 MB) now — checking the other missing file too. Will confirm when both land.

Glad v001_3 aggressive works — that's the lane we iterate from.

For the 20-min loop tomorrow: your NDI-live plan is probably the cleaner path. I can re-render the baseline with noise_anim=False (kills the jumps) + pre-encode to HAP (~4 GB direct Resolume drop, no Alley needed) as backup if useful — but I'll QC end-to-end on my Mac first this time, then loop you in.

Bob Turner: YouTube rate-limited my IP on yesterday's pulls (all 18 returned 403). Retrying with browser cookies tomorrow morning when the block likely clears. Was always a Sat-AM assembly target, never for the Monday test.

Have a good dinner.
